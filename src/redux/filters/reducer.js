import { COLORCHANGED, STATUSCHANGED } from "./actionTypes";

const reducer = (state = initialState, action) => {

    switch (action.type) {
        case STATUSCHANGED:
        case COLORCHANGED:
        default
}

}